# TaskFlow — Claude Code Project Memory

## Stack
<!-- TODO: Fill in the tech stack (language, framework, database, frontend) -->

## Run
```bash
pip install -r requirements.txt
python app.py
```

## Database
SQLite file: `taskflow.db` — initialised automatically on first run.

## Key files
| File | Purpose |
|------|---------|
| `app.py` | Flask routes |
| `db.py` | DB queries |
| `schema.sql` | DB schema |

## Rules
<!-- TODO: Add project-specific rules Claude should follow (e.g. "never use eval", "always use parameterized queries") -->

## Imports / banned patterns
<!-- TODO: List packages that must NOT be used -->
